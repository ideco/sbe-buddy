---
paths:
  - "reference/**"
---

`reference/simple-binary-encoding` is sbe-tool's source, checked out to confirm how it behaves. It is Apache-2.0 and this project is MIT: read it, then write our own. Never copy from it, never edit it, never build against it. A fact learned here that the implementation relies on goes into `docs/notes.md` with the file it was read from.
