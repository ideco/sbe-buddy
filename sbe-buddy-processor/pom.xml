<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.1.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.1.0 https://maven.apache.org/xsd/maven-4.1.0.xsd">
  <modelVersion>4.1.0</modelVersion>

  <parent>
    <groupId>net.concini</groupId>
    <artifactId>sbe-buddy-parent</artifactId>
  </parent>

  <artifactId>sbe-buddy-processor</artifactId>
  <name>sbe-buddy processor</name>
  <description>The annotation processor that turns an annotated package into its SBE schema, sbe-tool's flyweights and a record codec.</description>

  <dependencies>
    <dependency>
      <groupId>net.concini</groupId>
      <artifactId>sbe-buddy-api</artifactId>
    </dependency>
    <dependency>
      <groupId>net.concini</groupId>
      <artifactId>sbe-buddy-generator</artifactId>
    </dependency>
    <dependency>
      <groupId>net.concini</groupId>
      <artifactId>sbe-buddy-generator</artifactId>
      <type>test-jar</type>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <properties>
    <module.name>net.concini.sbebuddy.processor</module.name>
  </properties>
</project>
