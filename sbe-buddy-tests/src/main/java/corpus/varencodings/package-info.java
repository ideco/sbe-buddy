/**
 * The api's built-in var-data encodings, UTF-8 text, ASCII text and bytes, one
 * of them appended in a later version.
 */
@NullMarked
@SbeSchema(id = 1, version = 1)
package corpus.varencodings;

import org.jspecify.annotations.NullMarked;

import net.concini.sbebuddy.SbeSchema;
