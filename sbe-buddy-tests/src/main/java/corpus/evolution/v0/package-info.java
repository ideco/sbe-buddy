/**
 * The evolution schema as its first version wrote it: a group whose entry holds
 * a field, a composite and a nested group.
 */
@NullMarked
@SbeSchema(id = 5, version = 0)
package corpus.evolution.v0;

import org.jspecify.annotations.NullMarked;

import net.concini.sbebuddy.SbeSchema;
