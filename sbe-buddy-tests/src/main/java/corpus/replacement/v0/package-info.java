/**
 * The replacement schema as its first version wrote it: a quote over a
 * composite, in a union of its own.
 */
@NullMarked
@SbeSchema(id = 7, version = 0)
package corpus.replacement.v0;

import org.jspecify.annotations.NullMarked;

import net.concini.sbebuddy.SbeSchema;
