/**
 * A message replaced by a new template: version 1 adds {@code QuoteV2} over a
 * composite of its own beside {@code Quote}, since a composite cannot grow, and
 * a union takes both.
 */
@NullMarked
@SbeSchema(id = 7, version = 1)
package corpus.replacement;

import org.jspecify.annotations.NullMarked;

import net.concini.sbebuddy.SbeSchema;
