/**
 * Text in encodings other than ASCII: char arrays in ISO-8859-1 and UTF-8,
 * var-data in windows-1252 and UTF-16.
 */
@NullMarked
@SbeSchema(id = 1, version = 0)
package corpus.charsets;

import org.jspecify.annotations.NullMarked;

import net.concini.sbebuddy.SbeSchema;
